def open_file(text_txt)
    a= open(text_txt)
    for line in a:
        print line
    count=0
    for line in a:
        count+1=0
        print count 
    for line in a:
        line=line.rstrip()
        if line.startswith("zzzz")
            print line
        if not line.startswith("zzzz")
            continue

fname=raw_input("Enter a file name: ")

fahnd=open(fname)
for line in fhand:
    line=line.rstrip().upper()
    print line 
    sp
    
